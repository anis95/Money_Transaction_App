/**
 * Export user information
 */
export interface userInformation {
  name: string;
  phone: string;
  imageUri: string;
}

/**
 * Export form type
 */
export interface FormType {
  amount: string;
  fee: number;
  total: number;
}
